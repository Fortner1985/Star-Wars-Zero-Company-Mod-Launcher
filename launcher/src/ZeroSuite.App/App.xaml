<Application x:Class="ZeroSuite.App.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <Application.Resources>
    <SolidColorBrush x:Key="Ink"    Color="#E8EAED"/>
    <SolidColorBrush x:Key="Muted"  Color="#9AA0A6"/>
    <SolidColorBrush x:Key="Panel"  Color="#1F2226"/>
    <SolidColorBrush x:Key="Ground" Color="#15171A"/>
    <SolidColorBrush x:Key="Ok"     Color="#5BB974"/>
    <SolidColorBrush x:Key="Warn"   Color="#E8B339"/>
    <SolidColorBrush x:Key="Fail"   Color="#E05C5C"/>

    <Style TargetType="TextBlock">
      <Setter Property="Foreground" Value="{StaticResource Ink}"/>
      <Setter Property="FontFamily" Value="Segoe UI"/>
    </Style>
    <!-- WPF's default Button is a light-grey chrome control: on this dark
         ground its label was almost unreadable. Styled explicitly, including
         the disabled state, rather than relying on system colours. -->
    <Style TargetType="Button">
      <Setter Property="Foreground" Value="#F1F3F4"/>
      <Setter Property="Background" Value="#2E3236"/>
      <Setter Property="BorderBrush" Value="#4A4F55"/>
      <Setter Property="BorderThickness" Value="1"/>
      <Setter Property="FontFamily" Value="Segoe UI"/>
      <Setter Property="FontSize" Value="13"/>
      <Setter Property="Cursor" Value="Hand"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Border x:Name="Chrome" CornerRadius="4"
                    Background="{TemplateBinding Background}"
                    BorderBrush="{TemplateBinding BorderBrush}"
                    BorderThickness="{TemplateBinding BorderThickness}">
              <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                Margin="{TemplateBinding Padding}"/>
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="Chrome" Property="Background" Value="#3A3F45"/>
              </Trigger>
              <Trigger Property="IsPressed" Value="True">
                <Setter TargetName="Chrome" Property="Background" Value="#4A5058"/>
              </Trigger>
              <Trigger Property="IsEnabled" Value="False">
                <Setter TargetName="Chrome" Property="Background" Value="#232629"/>
                <Setter Property="Foreground" Value="#6B7075"/>
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <Style TargetType="CheckBox">
      <Setter Property="Foreground" Value="{StaticResource Ink}"/>
      <Setter Property="FontFamily" Value="Segoe UI"/>
      <Setter Property="Margin" Value="0,5,0,5"/>
    </Style>
  </Application.Resources>
</Application>
